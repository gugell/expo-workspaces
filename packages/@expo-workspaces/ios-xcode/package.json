{
  "name": "@expo-workspaces/ios-xcode",
  "version": "1.0.0",
  "private": true,
  "description": "iOS Xcode capability for expo-workspaces: pbx executor, scheme generation, .xcode.env, and the extension embed-cycle fix.",
  "license": "MIT",
  "main": "build/index.js",
  "types": "build/index.d.ts",
  "files": ["build", "templates", "README.md", "LICENSE"],
  "scripts": {
    "build": "tsc -b",
    "test": "npm run build && node --test build/**/*.test.js",
    "typecheck": "tsc -b"
  },
  "dependencies": {
    "@expo-workspaces/core": "workspace:",
    "@bacons/xcode": "1.0.0-alpha.32"
  },
  "peerDependencies": {
    "@expo/config-plugins": ">=56.0.0",
    "expo": ">=56.0.0"
  },
  "peerDependenciesMeta": {
    "@expo/config-plugins": { "optional": true },
    "expo": { "optional": true }
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "typescript": "~5.9.2"
  }
}
