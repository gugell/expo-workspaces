{
  "name": "@expo-workspaces/android",
  "version": "1.0.0",
  "private": true,
  "description": "Android Gradle/manifest capability for expo-workspaces: signing, SDK versions, gradle deps/repos, gradle.properties, and manifest config.",
  "license": "MIT",
  "main": "build/index.js",
  "types": "build/index.d.ts",
  "files": ["build", "README.md", "LICENSE"],
  "scripts": {
    "build": "tsc -b",
    "test": "npm run build && node --test build/**/*.test.js",
    "typecheck": "tsc -b"
  },
  "dependencies": {
    "@expo-workspaces/core": "workspace:"
  },
  "peerDependencies": {
    "@expo/config-plugins": ">=56.0.0",
    "expo": ">=56.0.0"
  },
  "peerDependenciesMeta": {
    "@expo/config-plugins": { "optional": true },
    "expo": { "optional": true }
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "typescript": "~5.9.2"
  }
}
