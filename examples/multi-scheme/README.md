# Multi-scheme example

Writes shared Xcode schemes for Debug and Release.

```sh
pnpm install
pnpm exec expo-workspaces plan
npx expo prebuild
```

Look in `ios/*.xcodeproj/xcshareddata/xcschemes/` after prebuild.
