# Widget example

Declares a WidgetKit extension. This is native project structure only — not a SwiftUI widget framework.

```sh
pnpm install
pnpm exec expo-workspaces plan
npx expo prebuild
```

Sources live in `targets/AcmeWidget/`.
